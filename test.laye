// no separation of namespaces, copy
// all exports for each declaration/namespace
import "stdio.h";
import "stdlib.h";

stdio::printf
stdio::malloc
stdlib::malloc

// just throw it all into the global namespace by default
import "stdio.h";
import "stdlib.h";
// or: make this the only valid syntax instead,
// for consistency with Laye imports
//

import * from "stdlib.h";

printf
malloc

int _foo();
#define foo() _foo()

foreign i32 get_foo() {
    return 10;
}

template<i32 N, T>
struct vec {
    T[N] elements;
}

template<i32 N, T>
vec<N, T> operator +(vec<N, T> lhs, vec<N, T> rhs) {
}
